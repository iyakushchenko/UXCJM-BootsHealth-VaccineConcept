---
description: Lean post-change gates before calling UX Studio work done
alwaysApply: true
---

# Post-change checklist (UX Studio)

After changes to **code**, **UI**, or **config**, before considering the task done:

1. `npm test` (fix failures).
2. `npm run build` (must stay green — Pages uses the same signal).
3. Update authoritative docs if behavior changed (`docs/product/*`).
4. UI-facing ships: strict FE audit **PROVEN** ([docs/product/FE_UI_UX_AUDIT.md](docs/product/FE_UI_UX_AUDIT.md)) — tests alone are BAD.
5. Chrome / playback: local MCP helpers or lean `npm run smoke` — not full marathon in CI.
6. User-visible / durable seams: `npm run notes:append -- --lane=<lane> --intent="…"`.
7. Commit when coherent (and allowed). Do not leave a green coherent tree uncommitted without a reason.

Full detail: [docs/product/POST_CHANGE_CHECKLIST.md](docs/product/POST_CHANGE_CHECKLIST.md) · versioning: [docs/product/VERSIONING.md](docs/product/VERSIONING.md) · Actions: [docs/product/CI_ACTIONS_BUDGET.md](docs/product/CI_ACTIONS_BUDGET.md).

Do **not** add GitHub Actions jobs to replace these local gates.
